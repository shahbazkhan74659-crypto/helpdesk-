// Well-known identity for the AI bot agent, seeded in prisma/seed.ts. Tickets
// get assigned to this user on arrival so agents can see AI is working them.
export const AI_AGENT_EMAIL = 'ai@helpdesk.internal';
export const AI_AGENT_NAME = 'AI';
